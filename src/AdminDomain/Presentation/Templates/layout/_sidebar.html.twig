<aside class="w-64 bg-gray-700 dark:bg-gray-900 flex-shrink-0">
    <div class="h-full flex flex-col">
        <nav class="flex-1 pr-4 py-6 space-y-1">
            <a href="{{ path('admin_dashboard', {'_locale': app.request.locale}) }}"
            class="flex items-center px-3 py-2 text-sm rounded-md text-gray-300 hover:bg-gray-700 hover:text-white transition-colors {{ app.request.attributes.get('_route') starts with 'admin_dashboard' ? 'bg-gray-800 text-white' : '' }}">
                {{ 'admin.navigation.dashboard'|trans }}
            </a>

            <a href="{{ path('admin_user_index', {'_locale': app.request.locale}) }}"
            class="flex items-center px-3 py-2 text-sm rounded-md text-gray-300 hover:bg-gray-700 hover:text-white transition-colors {{ app.request.attributes.get('_route') starts with 'admin_user_' ? 'bg-gray-800 text-white' : '' }}">
                {{ 'admin.navigation.users'|trans }}
            </a>

            <a href="#"
            class="flex items-center px-3 py-2 text-sm rounded-md text-gray-300 hover:bg-gray-700 hover:text-white transition-colors">
                {{ 'admin.navigation.content'|trans }}
            </a>

            <a href="#"
            class="flex items-center px-3 py-2 text-sm rounded-md text-gray-300 hover:bg-gray-700 hover:text-white transition-colors">
                {{ 'admin.navigation.settings'|trans }}
            </a>
        </nav>
    </div>
</aside>
