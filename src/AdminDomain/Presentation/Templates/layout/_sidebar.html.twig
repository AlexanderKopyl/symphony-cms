<aside class="w-64 h-screen bg-gray-900 text-white flex flex-col fixed">
    <div class="px-6 py-4 text-2xl font-bold border-b border-gray-700">
        CMS Admin
    </div>

    <nav class="flex-1 px-4 py-4 space-y-2 text-sm">
        <a href="{{ path('admin_dashboard', {'_locale': app.request.locale}) }}"
           class="flex items-center px-3 py-2 rounded hover:bg-gray-700 {{ app.request.attributes.get('_route') starts with 'admin_dashboard' ? 'bg-gray-800' : '' }}">
            Dashboard
        </a>

        <a href="{{ path('admin_user_index', {'_locale': app.request.locale}) }}"
           class="flex items-center px-3 py-2 rounded hover:bg-gray-700 {{ app.request.attributes.get('_route') starts with 'admin_user_' ? 'bg-gray-800' : '' }}">
            Users
        </a>
    </nav>

    <div class="p-4 border-t border-gray-700 text-xs text-gray-400">
        &copy; {{ "now"|date("Y") }} SymphonyCMS
    </div>
</aside>
